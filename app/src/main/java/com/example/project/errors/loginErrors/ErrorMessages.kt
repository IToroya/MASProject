package com.example.project.errors.loginErrors

enum class ErrorMessages(val rawValue: String) {
    EMAIL_ERROR_MESSAGE(rawValue = "Invalid Email"),
    PASSWORD_ERROR_MESSAGE(rawValue = "Invalid Password")
}
